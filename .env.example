# Server (Railway / Fly / Render / any Node host)
TYPESAFE_API_KEY=your_key_here
PORT=3001
HOST=0.0.0.0

# Client (Vercel) — set in Vercel Project → Settings → Environment Variables
# Full WebSocket URL to the game server, including path:
# VITE_WS_URL=wss://your-game-server.example.com/ws
