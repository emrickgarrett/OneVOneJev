# Game server (Railway / local production)
TYPESAFE_API_KEY=your_key_here
PORT=3001
HOST=0.0.0.0

# Optional: only if the static client is hosted on a different origin than the game server.
# Leave unset on Railway (same-origin /ws).
# VITE_WS_URL=wss://your-game-server.example.com/ws
